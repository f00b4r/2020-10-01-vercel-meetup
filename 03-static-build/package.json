{
    "devDependencies": {
        "parcel-bundler": "^1.12.4"
    },
    "browserslist": [
        "last 2 Chrome versions"
    ],
    "scripts": {
        "build": "parcel build index.html -d public",
        "vercel-build": "parcel build index.html -d public"
    }
}